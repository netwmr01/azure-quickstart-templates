{
  "$schema": "http://schema.management.azure.com/schemas/2015-01-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.1",
  "parameters": {
    "resourceAPIVersion":{
      "type":"string"
    },
    "vmName":{
      "type":"string"
    },
    "scriptsUri":{
      "type":"string"
    },
    "clusterParameters":{
      "type":"secureObject"
    }
  },
  "resources": [
    {
      "type": "Microsoft.Compute/virtualMachines/extensions",
      "name": "[concat(parameters('vmName'), '/prepareDisks')]",
      "apiVersion": "[parameters('resourceAPIVersion')]",
      "location": "[resourceGroup().location]",
      "properties": {
        "publisher": "Microsoft.OSTCExtensions",
        "type": "CustomScriptForLinux",
        "typeHandlerVersion": "1.4",
        "autoUpgradeMinorVersion": true,
        "settings": {
          "fileUris": [
            "[concat(parameters('scriptsUri'), '/scripts/prepare-director.sh')]",
            "[concat(parameters('scriptsUri'), '/scripts/prepare-director-conf.py')]",
            "[concat(parameters('scriptsUri'), '/scripts/setup-default.py')]"
          ],
          "commandToExecute": "[concat('bash prepare-director.sh ', parameters('clusterParameters').envName, ' ', parameters('clusterParameters').location, ' ', parameters('clusterParameters').subId, ' ', parameters('clusterParameters').tenantId, ' ', parameters('clusterParameters').clientId, ' ', parameters('clusterParameters').clientSecret, ' ', parameters('clusterParameters').userName, ' ', parameters('clusterParameters').passPhrase, ' ', parameters('clusterParameters').privateKey, ' ', parameters('clusterParameters').networkSecurityGroupResourceGroup, ' ', parameters('clusterParameters').networkSecurityGroup, ' ', parameters('clusterParameters').virtualNetworkResourceGroup, ' ', parameters('clusterParameters').virtualNetwork, ' ', parameters('clusterParameters').subnetName, ' ', parameters('clusterParameters').computeResourceGroup, ' ', parameters('clusterParameters').hostFqdnSuffix, ' ', parameters('clusterParameters').dbAddress, ' ', parameters('clusterParameters').dbUserName, ' ', parameters('clusterParameters').dbPassword, ' ', parameters('clusterParameters').masterType, ' ', parameters('clusterParameters').workerType, ' ', parameters('clusterParameters').edgeType, ' ', parameters('clusterParameters').dirUsername, ' ', parameters('clusterParameters').dirPassword)]"
        }
      }
    }
  ]
}
